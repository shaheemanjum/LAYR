{
  "name": "layr",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "testcases": "./node_modules/.bin/cypress run --headed --spec ./cypress/e2e/sequenced.cy.js --browser electron --reporter mocha-allure-reporter --env allure=true",
    "testcases_headless": "./node_modules/.bin/cypress run --spec ./cypress/e2e/sequenced.cy.js --reporter mocha-allure-reporter --env allure=true",
    "gen-allure-report": "allure generate allure-results --clean -o allure-report && allure open allure-report",
    "finalrun": "npm run testcases && npm run gen-allure-report",
    "finalrun_headless": "npm run testcases_headless && npm run gen-allure-report"
  },
  "author": "ANJUM",
  "license": "ISC",
  "devDependencies": {
    "allure-commandline": "^2.18.1",
    "chai": "^4.3.6",
    "cypress": "^10.9.0",
    "mocha-allure-reporter": "^1.4.0"
  },
  "dependencies": {
    "chai": "^4.3.6",
    "mochawesome": "^7.1.3"
  }
}